# DopeWars.js Modern

This is a modernized version of the original DopeWars.js game. The project now uses React and Bootstrap 5 instead of jQuery and the old Bootstrap 3 stack.

Open `index.html` in a browser to run the demo. The game logic was simplified for this modernization example.
